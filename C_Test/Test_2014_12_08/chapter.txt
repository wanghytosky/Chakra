第五章 运算符 表达时 语句
sizeof 以字节为单位返回
在过去一个字节8位，现在呦鞋字符集占更大的字节
typedef double real;//使real称为 double的别名
编译器可以自由选择先计算哪个参数值
y=n++ + n++
这个结果不确定 可能y＝6 n＝5等等
避免这种情况要遵守原则：
1.如果一个变量出现在一个函数参数中多次，不要使用增量或者减量
2.如果一个变量出现在一个表达式中多次，不要使用增量或者减量

一个完整的表达时结束或一个分号代表一个顺序点


